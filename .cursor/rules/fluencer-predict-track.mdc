---
description: 投资大V合订本：抓取并验证投资/财经账号预测，支持100万初始本金回测、语气仓位、复利年化收益、rf=2.8%夏普和净值曲线嵌入报告。
globs: 
alwaysApply: false
---

# 投资大V合订本

## 核心约定

使用本机 Chrome/Chromium CDP endpoint 访问公开页面和交互式登录会话。

默认 endpoint：

```text
http://127.0.0.1:15166
```

可覆盖方式：

- 环境变量：`FLUENCER_CDP_ENDPOINT`
- 脚本参数：`-Endpoint`
- 用户在任务中明确提供 endpoint

开始浏览前先运行辅助脚本：

```powershell
.\skills\fluencer-predict-track\scripts\ensure-chrome-cdp.ps1
```

不要读取、导出、打印或上传 cookie、localStorage、浏览器 profile、密码或 storage-state JSON。如果目标站点要求登录，让用户在浏览器窗口中完成登录，然后继续。

## 工作流程

1. 解析目标和验证截止日。除非用户另有指定，验证截止日使用任务当天。
   - 如果用户给的是 URL，直接使用该 URL。
   - 如果用户只给用户名、昵称或中文名，先用网页搜索或站内搜索查找候选账号。
   - 搜索时优先组合投资语境关键词，例如 `<用户名> 投资`、`<用户名> 股票`、`<用户名> A股`、`<用户名> 知乎 回答`。
   - 按搜索结果顺序检查候选账号，选择最靠前且公开内容中确实发表过投资/财经观点的账号。
   - 如果多个候选都符合，仍以搜索排序更靠前者为准；如果没有候选发表过投资观点，说明无法确定目标，不要强行分析同名无关账号。
   - 在报告中记录搜索词、候选判断和最终选择的目标 URL。
2. 使用 `scripts/ensure-chrome-cdp.ps1` 确认 CDP 可用。
3. 通过浏览器自动化连接该 CDP endpoint。
4. 打开目标用户的公开回答页或帖子列表。
5. 优先在浏览器上下文中调用页面自己的接口，不要一开始就模拟滚动。知乎常见接口示例：

   ```text
   https://www.zhihu.com/api/v4/members/<url-token>/answers?offset=0&limit=20&sort_by=created&include=data[*].content,voteup_count,comment_count,created_time,updated_time,question
   ```

6. 采集回答或帖子的链接、标题、创建时间、最后编辑时间和纯文本正文。
7. 建立预测候选池。优先选择具备下列特征的内容：
   - 有明确未来窗口：日期、年份、季度、明年、年底前、未来几个月等。
   - 有可验证结果：价格、指数点位、政策事件、宏观数据、公司或行业事件。
   - 有清晰方向或阈值：上涨/下跌、突破/不突破、发生/不发生、跑赢/跑输。
8. 排除下列内容：
   - 最后编辑时间晚于预测事件或验证窗口。
   - 预测过于模糊，必须发明口径才能验证。
   - 关键判断主要依赖无法可靠读取的图片。
   - 预测目标日在验证截止日之后。除非用户要求列出，否则这类内容只标记为 `尚未到期`，不计入准确率。
9. 除非可验证候选不足，否则每个目标至少选择 20 条。多目标任务按目标分别计数，不要把不同账号的样本合并后凑满 20 条。每个目标的样本都要尽量覆盖不同年份或时间段，不要只选最近内容。
10. 使用外部公开来源验证每条预测。优先级：
    - 官方统计、央行、监管机构、交易所、公司公告和财报。
    - FRED、BIS、World Bank、IMF、NBS、Federal Reserve、CSRC/CFFEX。
    - 官方数据不可得时，可使用 Yahoo Finance、Stooq、Trading Economics 或其他可引用行情数据。
11. 如果用户要求回测或净值曲线，将可交易预测转换成交易信号并回测。默认初始本金为 1,000,000；若用户未指定币种，以报告中的基准币种记账。只把能映射到明确标的、方向和入场时间的数据纳入净值曲线；无法可靠映射的预测保留在复盘表中，但不进入回测。
12. 回测完成后必须绘制净值曲线图，保存到报告同级或 `reports/assets/` 下，并用 Markdown 图片语法嵌入报告正文，例如 `![净值曲线](assets/<chart>.png)`。报告中还要列出回测参数、数据源、排除样本和核心指标。
13. 保存 Markdown 报告：

    ```text
    reports/fluencer-predict-track-<yyyy-mm-dd>-<target>.md
    ```

14. 最终回复中总结结论，并给出本地报告路径。

## 判定口径

- `准确`：方向和时间窗口都基本正确。
- `部分准确`：方向正确，但时间、幅度或适用范围有明显偏差。
- `不准确`：方向、阈值或事件判断明显错误。
- `无法验证`：公开数据不足，或原预测缺少可量化口径。
- `尚未到期`：预测目标日在验证截止日之后。

如果给出分数或命中率，必须说明这是样本得分，不是统计意义上的完整胜率，除非用户明确要求并且确实处理了完整语料。

## 回测口径

当任务包含回测、净值曲线、模拟跟单、收益曲线或类似要求时，使用下列默认口径，除非用户另有指定：

- 初始本金：`1,000,000`。
- 入场时间：预测发布后的下一个可交易日开盘价；如果只有收盘价数据，则使用下一个可交易日收盘价，并在报告中说明。
- 退出时间：优先使用预测文本中的明确窗口；若没有明确窗口，默认分别计算 20 个交易日主回测，并可在附录补充 5/60 个交易日敏感性结果。
- 方向：看多/买入/加仓为多头；看空/卖出/清仓为减仓或空仓。默认不做空，除非用户明确允许 long-short。
- 仓位：根据语气强弱给仓位，默认弱信号 5%、中等信号 10%、强信号 20%，单标的上限 25%，组合总仓位上限 100%。风险提示类信号用于降仓或保护，不直接开新仓。
- 成本：默认计入单边 0.1% 交易成本；如果数据市场有更合理费率，在报告中说明。
- 无风险利率：默认使用年化 `rf = 2.8%` 计算夏普；若用户指定其他无风险利率，以用户指定为准。日频回测默认按 252 个交易日折算日无风险收益。
- 数据：尽量获取投资标的历史 OHLCV 或收盘价数据。优先使用交易所、官方行情、公司公告、Stooq、Yahoo Finance、Trading Economics、AkShare/Tushare 等可复查来源；每个标的都要记录数据源和 ticker/代码映射。
- 标的映射：把自然语言标的映射成可交易 ticker、指数或 ETF 代理，例如 `英伟达 -> NVDA`、`纳指/QQQ -> QQQ`、`黄金 -> 现货黄金或 GLD/IAU`。映射不唯一时选择流动性最高且最贴近预测语义的代理，并在报告中列出。
- 冲突信号：同一作者、同一标的、同一方向的信号可合并但不得超过仓位上限；反向信号默认先平旧仓，再按新信号建仓。
- 输出指标：至少给出期末净值、累计收益、年化收益（复利 CAGR）、最大回撤、年化夏普、使用的无风险利率、胜率、平均单笔收益、参与交易数、未纳入回测的预测数和排除原因。
- 输出图表：必须绘制净值曲线；若有基准指数，优先同时绘制基准曲线。图片文件与 Markdown 一起保存在报告目录中，并用相对路径嵌入。

## 报告结构

报告应包含：

- 任务范围、目标 URL、采集日期、验证截止日。
- 如果目标来自用户名搜索，写明搜索词、候选账号和最终选择理由。
- 采集方式、CDP endpoint、采集到的回答数量和限制。
- 筛选规则和排除规则。
- 表格：回答日期、最后编辑日期、回答链接、预测内容概括、验证数据、来源、判定。
- 如执行回测：交易信号表、标的映射表、仓位规则、历史数据来源、交易成本、无风险利率、年化收益（复利 CAGR）、年化夏普、净值曲线图、回测指标和排除样本说明。
- 结果总结：必须在表格前后都提供足够多的总结，而不是只给表格。至少包括样本判定分布、可计分与不可计分数量、总体准确性结论、哪些类型的预测更准、哪些类型更弱、最常见的误判模式、可追溯性风险、尚未到期内容对结论的影响，以及读者应如何使用这些结论。
- 多目标报告要按目标分别总结，不要只写一个混合总评。每个目标都要单独给出强项、弱项、常见误判模式和注意事项。

正文要概括和转述回答内容。不要长篇引用知乎或其他受版权保护页面的原文。
